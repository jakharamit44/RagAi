# Student Portal Widget (Phase 10)

Lightweight, embeddable web chat widget wired to `/api/v1/ask` and `/v1/chat/completions`:
- Supports streaming responses via Server-Sent Events (SSE)
- Displays verified document citations with page numbers and snippet previews
- Seamlessly mounts inside existing university portal or LMS
